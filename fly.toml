# fly.toml app configuration file generated for imagen-v2 on 2025-09-11T12:25:46Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'imagen-v2'
primary_region = 'gru'

[build]

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']
  # 🔑 CLAVE: Define el tiempo máximo que una máquina inactiva esperará
  # antes de ser apagada (por defecto es 5m). '4m' para 4 minutos.
  stop_machine_timeout = '4m'

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
